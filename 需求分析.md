<!--
  文档性质：Alpha 需求基线与实现状态核验
  说明：原文中的条目属于前序需求/状态声明；本次修订依据仓库代码、测试、配置和接口文档重新核验。
-->

# 空地异构多智能体指挥与控制系统

## 1. 文档边界与版本基线

### 1.1 文档用途

本文件同时承担四项职责：

1. 将前序需求从简略功能清单细化为可实现、可验证的功能要求。
2. 对照当前仓库的代码、测试、配置、变更记录和端侧包，纠正“已完成/未完成”标记。
3. 固化地面站、端侧通信包及端侧 ROS 功能包之间的数据接口边界。
4. 定义 Alpha 版本的范围、平台兼容性、交付计划和验收门禁。

前序文档内容不是新的操作指令，而是本次核验的输入基线；用户提出的分析、补充、纠错和开发计划要求是本次修订目标。

### 1.2 当前发布基线与历史目标

2026-09-03 发布工程基线为地面站 **v0.23.0**。交付 Windows 10/11 x64 安装包、Ubuntu 20.04/22.04 x64 自解压安装包、地面站便携 ZIP 和独立端侧配套 ZIP；各端侧包版本沿用当前包内声明。

安装包须携带应用依赖，允许指定可写目录及无人值守安装；便携版须先用 uv 或 Python 3.10–3.13 的 pip 环境安装依赖。config/、data/ 保存于软件根目录，升级/卸载保留已有内容，发布默认设备列表为空。发布能力、目录迁移、离线启动、数据排除及四系统验收要求见 [发布指南](docs/RELEASING.md)；实际证据见 [验证记录](docs/RELEASE_VALIDATION.md)。

以下表格保留原 Alpha 阶段历史目标，不作为当前软件版本或当前实机验收结论。

| 项目 | 当前基线 | Alpha 目标 |
| --- | --- | --- |
| 地面站 | v0.13.1 | 保持 v1 协议兼容，完成端侧 PGM 下载、断点恢复及已保存地图 PCD/PGM 同步融合 |
| MQTT 端侧 | epgeneral_mqtav v0.3.0 | Ubuntu 20.04 + ROS1 Noetic 验收 |
| UDP 遥测 | epgeneral_udp_telemetry v0.2.2 | Noetic 话题与频率实机验证 |
| 实时建图 | epgeneral_map_stream v0.1.0 | 真实点云/位姿联调，多设备由地面站编排 |
| 任务协调 | epgeneral_task_control v0.1.0 | 连接参考设备运动控制适配器并完成安全停止闭环 |
| 视频 | epgeneral_video_srt v0.1.0 | Noetic、GStreamer/SRT、真实摄像头和 SRT 拉流验收 |
| ROS2 | 尚未提供 ROS2 包 | 只交付核心复用和迁移设计，不作为 Alpha 可运行门禁 |

### 1.3 状态定义

- **代码已完成**：仓库中已有可运行实现，且有对应单元/UI/协议测试或明确的 localhost 契约测试。
- **协议/自动化测试已完成**：协议、状态机或存储已验证，但真实 ROS、传感器、网络或执行器尚未验证。
- **部分完成**：已有一部分实现，但功能边界、设备适配、跨页面联动或实机闭环仍缺失。
- **Alpha 必须完成**：不满足则不能宣称 Alpha 可验收。
- **Beta/后续**：不阻塞本 Alpha，但必须在计划或风险清单中保留。

## 2. 系统范围与总体架构

系统由地面站指控软件、端侧通信包、端侧传感器/执行功能包、网络基础设施和本地数据仓储组成。

```text
操作者
  |
  v
地面站 PySide6
  |-- 设备/MQTT/UDP/SRT 监测
  |-- 地图仓储、实时建图、点云/PGM 显示
  |-- 任务仓储、冲突检查、下发与执行监控
  `-- 指控大屏
       |
       +-- MQTT 1883 <--------- epgeneral_mqtav <--------- MAVROS 状态/电池/任务
       +-- UDP 14560 <--------- epgeneral_udp_telemetry < ROS 位姿/IMU/状态
       +-- SRT UDP 9000 ------- epgeneral_video_srt <--- ROS 图像/GStreamer
       +-- UDP 14561/14562 <--- epgeneral_map_stream <-- PointCloud2/Odometry/外参
       `-- UDP 14563/14564 <--- epgeneral_task_control <-> 运动控制适配器
```

所有端侧包使用 `epgeneral_device_config` 的设备 ID/IP。地面站 `config/devices.json` 与端侧身份文件不一致时，禁止把数据自动归属于设备。

## 3. 主要功能需求

### 3.1 FR-DEV 设备管理与状态监测

**输入**：设备名称、类型、唯一 ID、IPv4/IPv6、SRT 端口/延迟、能力和可选标定信息；端侧 MQTT/UDP/SRT 数据。

**功能要求**：

1. 支持新建设备、重复 ID 检查、IP 合法性校验、异步连通性测试、批量删除、搜索和类型/状态筛选。
2. 设备类型改为可扩展能力目录，不能由固定的 `UGV/UAV/AMR/USV` 枚举限制后续设备类型；类型与能力、可用传感器、执行器和显示卡片分离。
3. 设备静态档案与运行时快照分离保存。运行时状态包括 MQTT、UDP、健康、电量、FCU、飞行模式、任务状态、定位状态和最后更新时间。
4. 状态卡按设备能力配置，支持可用、不可用、未知和数据年龄；保留最近 500 条设备及链路日志。
5. 详情页通过系统 FFmpeg 提供按需 SRT 视频，并在关闭、切换设备、切换页面和退出程序时终止接收进程。
6. 提供二维视频/图像预览和三维地图标记统一接口。三维标记至少包含设备 ID、名称、位置、坐标系、朝向、状态颜色和最后更新时间。
7. 支持设备间初始坐标关系和地图坐标标定；未完成标定时禁止把来自不同坐标系的轨迹直接拼接。

**输出**：设备列表、详情快照、状态卡、日志、视频状态、地图标记和模块健康状态。

**异常**：配置损坏进入只读保护；MQTT、UDP、SRT、地图和任务模块分别降级，不得阻止本地编辑；超时数据显示 warning/error 或 unknown，不伪造零值。

**当前状态**：设备 CRUD、ping、MQTT/UDP 状态卡、日志、SRT/FFmpeg 生命周期和类型模板已完成；坐标初始化、二维预览和跨页面三维标记为部分完成。

**Alpha 验收**：至少一台真实设备完成 MQTT、UDP、视频和地图标记闭环；自定义类型和设备能力配置可持久化；无标定关系时有明确阻止提示。

### 3.2 FR-MAP 地图生命周期与多设备建图

**输入**：地图名称、设备集合、PCD、ROS map_server YAML/PGM、实时点云帧、位姿和坐标标定。

**功能要求**：

1. 支持地图创建、搜索、重命名、删除到回收目录、导入/替换 PCD、导入/替换 PGM/YAML、详情查看、视角操作和 ZIP 导出。
2. 校验 PCD 的 XYZ 有限性、点数和范围；校验 PGM 的分辨率、原点、yaw、图像尺寸、占据阈值和 free/occupied/unknown 分类。
3. 维护地图 schema、稳定地图 ID、图层状态、创建设备快照、地图指纹和最近建图会话。
4. 实时建图使用 `ccs-map-stream-v1`，支持开始/停止协商、点云同步、帧分片、乱序/重复处理、CRC32/zlib 校验、坐标变换、体素融合、预览、检查点和中断恢复。
5. 多设备建图须为每台设备建立独立 session，记录设备、时间、frame、标定、点数和轨迹；地面站在统一坐标系下进行点云配准和融合。
6. 支持点云后处理、PGM 生成或导入、PGM 拼接和融合结果质量检查。任何缺少标定、frame 不一致或地图版本不一致的结果不得静默合并。
7. 地图查看器统一支持点云、栅格、叠加、设备位置、任务路径、冲突和执行标记。

**输出**：可复现的地图目录、`map.json`、PCD/PGM/YAML、轨迹 CSV、会话检查点、融合统计和导出 ZIP。

**异常**：数据报缺片、CRC/zlib/尺寸/有限性/来源校验失败时丢弃整帧；建图中断保留临时结果；正式提交失败时恢复旧地图。

**当前状态**：地图 CRUD、PCD/PGM 导入显示、插件化离线融合、单机/多机建图、PCD 转 PGM、端侧 PGM 下载协调和二维栅格融合已完成地面站代码/协议测试；真实端侧 PGM 文件服务、多设备 ROS 联调和自动配准仍为 Alpha 缺口。

**Alpha 验收**：一台设备完成真实建图并保存正式地图；两台模拟/localhost 设备可同时产生独立 session 并在统一 frame 下融合；中断、缺片和旧地图回滚测试通过。

### 3.3 FR-TASK 任务规划、下发与执行

**输入**：任务名称、地图、设备集合、每台设备的子任务、航点、速度、启动延迟和安全阈值。

**功能要求**：

1. 支持任务创建、地图绑定、设备选择、子任务编辑、保存、删除、搜索、审计和执行历史。
2. PCD 视图支持地面平面选点；PGM 视图按 resolution/origin/yaw 转换坐标，只允许 free 栅格；支持 XYZ 精确编辑、增删、排序、默认高度、速度和启动延迟。
3. 每个有效子任务包含 2-500 个有限 XYZ 航点，并记录设备快照、地图 frame、地图指纹、revision 和下发 revision。
4. 按连续线性航段计算多设备时空冲突，支持水平/垂直距离、时间裕量、冲突显示和带理由的强制执行审计。
5. 使用 `ccs-task-control-v1` 完成 prepare/chunk/commit/execute/cancel/stop、CRC32/zlib、分片、ACK、重试、缺片补发、幂等 request ID 和 revision 校验。
6. 多设备执行必须先完成所有设备的目标 revision 下发，再使用统一 UTC `scheduled_at` 启动；设备锁阻止同一设备并发任务。
7. 接收并展示 scheduled/running/completed/stopped/failed、心跳、航点进度、位置、错误码和消息；任务停止必须进入安全状态并释放锁。

**当前状态**：任务页面、PCD/PGM 选点、子任务列表、冲突检查、下发/执行/停止服务、审计和 localhost 协议测试已完成；真实运动控制适配器、ROS Noetic 话题和实机闭环未完成。

**Alpha 验收**：一台参考设备完成任务下发、统一启动、进度回传和安全停止；两台模拟设备共同执行和心跳超时故障测试通过。

### 3.4 FR-DASH 指控大屏

**功能要求**：

1. 单页面显示在线设备、MQTT/UDP 模块健康、健康状态、电量、FCU、任务、位置和姿态趋势。
2. 支持地图切换、点云/栅格/叠加图层、视角复位、适配全图、扫描动画、设备位置/朝向/轨迹、任务路径和冲突显示。
3. 支持任务选择、开始任务、终止任务和执行状态/日志显示；任务控件必须复用共享任务服务，不能产生第二套状态。
4. 支持设备栏、状态栏和控制台收起、响应式布局、全屏和 Esc 退出；窗口窄化时不得遮挡或伪造数据。
5. 没有有效地图、设备、遥测或任务时显示明确空态，禁止使用模拟位置或模拟执行结果。

**当前状态**：大屏实时监控、趋势、图层、全屏、任务联动和开始/终止控制已完成；真实设备闭环和统一告警验收仍属于 Alpha 工作。

### 3.5 FR-COMM 通信、可靠性与运维

1. 固化 MQTT、UDP、SRT 的端口、schema、超时、重试、日志和故障隔离规则。
2. 所有持久化写入采用临时文件校验、fsync 和原子替换；任务执行和建图会话保留审计/事件日志。
3. 统一检查设备 ID/IP、descriptor hash、地图 fingerprint、任务 revision、坐标 frame 和 NTP 时间。
4. 防火墙、端口占用、配置解析、OpenGL、ROS 话题缺失和 GStreamer 故障必须可诊断并局部降级。
5. Alpha 仍允许可信局域网明文 MQTT/UDP/SRT，但必须记录无认证、无 TLS、无 ACL、无拥塞控制的安全风险；认证加密列为后续版本。

## 4. 实现状态核验矩阵

| 功能域 | 已完成证据 | 当前缺口 | Alpha 动作 |
| --- | --- | --- | --- |
| 设备 | `DeviceConfigRepository`、MQTT/UDP 数据源、状态卡、SRT、UI 测试 | 坐标关系、二维预览、地图页标记不完整 | 扩展能力目录、标定模型、统一 preview/marker API |
| 地图 | `MapRepository` schema 5、PCD/PGM、VisPy、融合插件、PGM 下载/融合、单机/多机 `MapBuildingService`、协议测试 | 真实 ROS PGM 文件服务、实机联调、自动配准 | 一台实机 + 两台模拟设备；建立融合质量门禁 |
| 任务 | `TaskRepository`、选点、冲突检测、`TaskExecutionService`、localhost 测试 | 设备专属运动适配器和真实执行反馈 | 交付参考设备适配器，验证安全停止和进度真值 |
| 大屏 | 实时状态、趋势、图层、标记、任务选择/开始/终止 | 真实链路告警和跨页面标记一致性 | 以共享服务为唯一真值，完成实机验收 |
| 端侧通信 | MQTT、14560、14561/14562、14563/14564 协议/纯 Python 测试 | Noetic、ROS 话题、传感器、相机和执行器联调 | Ubuntu 20.04 + ROS Noetic 现场验证 |
| 平台发布 | v0.23.0 发布脚本、冻结适配和安装模板 | 四系统离线安装与桌面实机验证按验证记录逐项验收 | Windows 10/11、Ubuntu 20.04/22.04 发布检查 |
| ROS2 | `ros_adapters.py` 仅有转换边界 | 没有 ament/rclpy 功能包 | 只完成迁移设计和 IDL 草案 |

## 5. 数据接口定义分析

详细字段以 `docs/EDGE_DEVICE_INTERFACES.md` 为准；本节规定链路边界和验收重点。

### 5.1 指控系统与端侧链路

| 链路 | 方向 | 端口 | 载荷与职责 | Alpha 检查点 |
| --- | --- | --- | --- | --- |
| MQTT 摘要 | 端侧 -> 地面站 | TCP 1883 | JSON schema 1.0，presence/heartbeat/status | ID/IP、QoS 1、Last Will、2/5 秒超时 |
| UDP 遥测 | 端侧 -> 地面站 | UDP 14560 | MessagePack `ccs-udp-telemetry-v1`，20/5/1 Hz | descriptor hash、session/sequence、有限数值 |
| SRT 视频 | 地面站 Caller -> 端侧 Listener | UDP 9000 | baseline H.264/MPEG-TS/SRT，无音频 | 开关、断流、切页释放和真实摄像头 |
| 建图控制 | 地面站 -> 端侧 | UDP 14561 | MessagePack `ccs-map-stream-v1` start/stop | request 幂等、ACK、来源 IP、参数上限 |
| 建图数据 | 端侧 -> 地面站 | UDP 14562 | 同协议 heartbeat/status/cloud_chunk | 1400 字节分片、CRC32/zlib、乱序/缺片 |
| 任务控制 | 地面站 -> 端侧 | UDP 14563 | MessagePack `ccs-task-control-v1` prepare/chunk/commit/execute/cancel/stop | 800 字节任务分片、revision、UTC |
| 任务状态 | 端侧 -> 地面站 | UDP 14564 | ACK、heartbeat、status、waypoint progress | 状态/进度真值、心跳超时、安全失败 |

公共要求：MessagePack 信封必须包含 schema/protocol、设备/任务/会话标识、消息类型、request ID 或 sequence、时间戳和 payload；端侧不得接受错误设备、错误来源 IP、非法序列、超限数据或不一致 revision。已有 v1 协议不做破坏性变更，扩展使用新字段或新 schema。

### 5.2 端侧通信包与功能包链路

| 通信包 | 上游功能包/话题 | 下游 | 关键接口 |
| --- | --- | --- | --- |
| `epgeneral_mqtav` | `/mavros/state`、`/mavros/battery`、可选任务话题 | MQTT Broker | 设备健康、电量、FCU、任务状态 |
| `epgeneral_udp_telemetry` | Pose/IMU/PointCloud2/地图状态/String | UDP 14560 | 动态消息类型、字段路径、描述哈希、采样窗口 |
| `epgeneral_map_stream` | PointCloud2、Odometry、静态 `body<-sensor` 外参 | UDP 14562 | 同步帧、XYZ float32、坐标变换、分片 |
| `epgeneral_task_control` | UDP 14563；XML 任务仓储 | ROS command/feedback | `TaskExecutionCommand`、`TaskExecutionFeedback`、UTC 调度 |
| `epgeneral_video_srt` | 配置的 Image/CompressedImage 话题 | GStreamer/SRT | 分辨率、帧率、编码、UDP 9000 Listener |
| `epgeneral_device_config` | YAML 身份/能力/标定 | 所有端侧包 | 唯一 device ID、IP、能力和坐标基线 |

`epgeneral_task_control` 不直接解锁飞控或调用 MAVROS，设备专属运动控制适配器负责实际执行。14560 的全局位姿只用于地面站实时地图标记；14564 的任务进度和状态才是任务执行真值。

## 6. 多设备与平台兼容性

### 6.1 指控系统兼容矩阵

下表“支持”表示目标范围；尚未完成的实机验收不得据此标记为通过。详细结果以发布验证记录为准。

| 平台 | Alpha 状态 | 约束与验证 |
| --- | --- | --- |
| Windows 10/11 x64 | 支持 | Python 3.10–3.13、PySide6 6.8.3、FFmpeg/SRT、可用 OpenGL、TCP/UDP 防火墙 |
| Ubuntu 20.04 x64 | 支持 | Python 3.10–3.13 虚拟环境、Qt6、OpenGL、systemd/桌面启动 |
| Ubuntu 22.04 x64 | 支持 | Python 3.10–3.13、Qt6、OpenGL、Wayland/X11 回归 |
| 其他桌面系统 | 非 Alpha 门禁 | 可运行性不承诺，需单独评估 Qt Multimedia/OpenGL |

### 6.2 端侧兼容矩阵

| 平台 | Alpha 状态 | 设计要求 |
| --- | --- | --- |
| Ubuntu 20.04 + ROS1 Noetic | 主线支持 | catkin、Python 3.8+、MAVROS、GStreamer、真实传感器/执行器验收 |
| Ubuntu 18.04 + ROS Melodic | 旧版维护 | 保留现有说明，不作为 Alpha 发布门禁 |
| Ubuntu 22.04 + ROS2 Humble | 设计目标 | ament/rclpy、launch.py、ROS2 IDL，Alpha 不要求可运行实现 |
| Ubuntu 24.04 + ROS2 Jazzy | 设计目标 | Python/Qt/GStreamer/消息接口风险清单，Alpha 不要求可运行实现 |

ROS2 迁移必须保持地面站 wire contract 不变，将协议核心、校验、状态机、存储和消息转换从 `rospy` 中抽离；ROS1 的 `.msg`、launch 和参数分别映射为 ROS2 IDL、launch.py 和参数文件。

## 7. Alpha 完成清单与验收门禁

Alpha 必须完成：

1. 可扩展设备类型、能力配置、初始坐标/标定、二维预览和统一三维标记。
2. 一台真实设备实时建图闭环；多设备独立 session、坐标校验和仿真融合。
3. PCD/PGM 后处理、地图版本指纹和失败回滚。
4. 一台真实设备任务下发、UTC 启动、状态/进度回传和安全停止。
5. Ubuntu 20.04 + ROS Noetic 端侧 catkin 构建及所有端侧包基础联调。
6. Windows 10/11、Ubuntu 20.04/22.04 地面站依赖和回归验证。
7. 协议故障注入、日志、NTP、防火墙、重启清理和文档一致性检查。

Alpha 通过条件：一台参考设备完成 MQTT、14560、SRT 9000、14561/14562、14563/14564 全链路；多设备任务和地图流程通过仿真/localhost 契约测试；所有已知限制和安全风险均有发布记录。

## 8. 相关计划文档

- `docs/ALPHA_DEVELOPMENT_PLAN.md`：总体里程碑、依赖、风险和发布门禁。
- `docs/GROUND_STATION_DEVELOPMENT_PLAN.md`：指控系统分模块开发任务。
- `docs/EDGE_PACKAGE_DEVELOPMENT_PLAN.md`：端侧功能包、ROS Noetic 验收和 ROS2 迁移设计。
