# 版本更新记录

本项目采用 `主版本.次版本.修订号` 三段式版本号。

## v0.23.0 - 2026-09-03

- 增加 Windows Inno Setup、Ubuntu 自解压安装包、本地便携 ZIP 和独立端侧配套 ZIP 构建入口；支持指定安装位置与无人值守参数。
- 发布使用独立干净配置和明确资源清单，生成版本清单与 SHA-256；安装版携带 Python、PySide6、Open3D、FFmpeg 和第三方许可。
- 统一软件根目录与内置资源目录，业务配置、数据、主题 INI 和轮转日志随软件目录保存；升级和卸载保留用户配置与数据。
- 增加冻结融合 worker、FFmpeg 子进程运行环境适配、目录写权限检查及 Linux 运行中升级保护。
- 固定 Python 3.10.19 构建基线与 PySide6 6.8.3，源码支持 Python 3.10–3.13；更新 uv 锁文件和发布依赖组。
- 重整 README，拆分使用/发布指南并增加独立验证记录。四系统实机验证以验证记录为准，端侧协议及各包版本不变。

## v0.22.9 - 2026-09-03

- 补齐首页实时建图、UDP 任务控制、设备重定位、地图仓储和任务仓储图标；设备列表新增设备总数、在线设备、需关注及 MQTT 状态图标，保留文字、数值和状态。
- 应用 SVG 统一从 `icons/app_icons/` 加载，地图统一为大小写准确的 `mapStorage`，修正 `taskStrorage` 为 `taskStorage`；拼接 SVG 仅保留后追加的新设计，不改变素材配色。
- 抽取共享卡片图标控件，日夜切换不重建业务数据或重置筛选、编辑选择；顶栏版本号后新增 32 px 实验室 Logo 与 AADCL 品牌文字。
- 运行时与项目元数据同步至 v0.22.9，端侧包版本、通信协议和端口保持不变；补充资源、状态保留、版本及窄窗口增量测试。

## 端侧包规范化 - 2026-09-02

- 端侧固定为 device_config、map_stream、mqtav、relocalization、task_control、udp_telemetry 和 video_srt 七包；删除旧 Go2 bridge 与 ros_udp_telemetry。
- 六个主功能包的运行 YAML 集中到 epgeneral_device_config；deploy 与 documents 明确仅保留在指控端。
- Go2 profile 改用 Livox/LIO 原生话题，MQTAV 支持禁用无数据源的电池订阅；wire 协议不变。
- 七个端侧包统一增加补丁版本，指控平台仍为 v0.22.8。

## v0.22.8 - 2026-09-01

- 首页四张全览卡新增在线设备、离线设备、地图仓储和任务执行语义图标，数值与标题文字保持可见。
- NTP、MQTT Broker、MQTT 数据订阅、UDP 高频遥测和 FFmpeg/SRT 子系统卡新增主题化图标；日间/夜间切换只刷新图标，不重置统计值或运行状态。
- 新增主题化首页 SVG、无主题资源加载和图标/UI 回归测试；平台升级至 v0.22.8，端侧包、通信协议 schema 和端口保持不变。

## v0.22.7 - 2026-08-28

- 联合建图成果提交后立即关闭所有设备协调器并释放暂存成果、融合状态和导航锁；地图页同步恢复重新建图、计时及页面切换状态。
- 重定位十字星与设备 ID 绑定，同一地图的启动、待选点和解算流程按设备互斥；无有效协商会话时禁止下发地图，避免页面重进时的会话竞态。
- 重定位十字星改为 3 px 圆角线条。设备轨迹使用按 profile 解析的本地 odom 位姿，并在重定位、临时无 marker 和同地图页面切换期间保留。
- 修复 VisPy 冻结 visual 不允许动态增加轨迹标记属性导致地图二级页刷新报错的问题；轨迹可见状态改由查看器内部状态表维护。
- 指控大屏的位置与姿态统一来自本地 odom 位姿，时间轴从 5 秒逐步扩展至固定 60 秒；设备列表、状态、地图、轨迹和趋势统一使用稳定设备颜色，并移除“解锁”和“FCU”字段。
- 平台升级至 v0.22.7。`epgeneral_relocalization` 保持 v0.2.2，`epgeneral_map_stream` 保持 v0.12.0；端侧包、`ccs-relocalization-v1`、`ccs-map-stream-v2`、遥测 wire schema 和端口均不变。

## v0.22.6 - 2026-08-28

- 新建地图弹窗开放 `ccs-map-stream-v2` 多设备联合遥控建图，要求至少两台设备，支持指定主设备及填写 `主设备 map <- 从设备 map` 的 XYZ/RPY 外参；PGM 联合模式拒绝非零 Roll/Pitch。
- 地面站为每台设备建立独立 v2 会话，聚合实时预览，按完整外参融合 PCD、按 X/Y/Yaw 融合 PGM，再原子提交 PCD/PGM/YAML。主设备失败中止作业，从设备失败可剔除继续。
- 地图二级页为设备卡、设备形状、实时轨迹和日志建立稳定统一配色；地图自身 XYZ 轴加粗并增加箭头。
- 建图与重定位日志改为左右排列；建图日志新增设备 ID、级别和方向列，支持设备筛选、自动滚动及不删除底层记录的一键清除。
- 平台升级至 v0.22.6，`epgeneral_map_stream` 升级至 v0.12.0。`ccs-map-stream-v2` schema 2、artifact schema 1、UDP 14561/14562 和 TCP 14600 保持不变。

## v0.22.5 - 2026-08-27

- 任务二级页移除重复的点云/栅格选点下拉框，直接使用共享地图查看器的图层控件；栅格选点继续校验自由栅格，叠加模式按点云模式保存以兼容现有任务文件。
- 点击设备卡只切换设备并收起任务点编辑区，仅“创建子任务”展开任务点列表；设备卡不再显示端侧原始状态和消息。
- 任务日志统一为地图页样式，聚合审计与全部执行事件，显示毫秒时间及准确的设备、事件、执行 ID 和消息；支持自动滚动和不删除磁盘记录的会话内视图清除。
- “保存下发”按服务、地图复核、子任务编辑、航点数量和选点状态统一控制；单设备执行按钮在执行期间切换为红色“终止任务”，终态后恢复。
- 所有 `task_summary` 状态均写入日志并触发页面刷新。平台升级至 v0.22.5；任务 schema、`ccs-task-control-v2` 消息格式、UDP 14563/14564 端口及端侧包版本保持不变。

## v0.22.4 - 2026-08-27

- 修复选择设备后位置与姿态趋势没有立即渲染的问题，首个遥测样本以可见数据点显示，后续样本形成连续曲线。
- 统一三维地图设备图层为最高渲染层，设备形状、回退标记、朝向轴和轨迹不再被 PGM 遮挡。
- 指控大屏取消光标坐标选项，地图页和任务页继续保留共享查看器的光标坐标功能。
- 拦截 VisPy 画布的 Esc 默认关闭行为，退出大屏全屏后恢复并重绘地图画布。
- 增加曲线、设备图层、光标能力和 Esc 画布生命周期增量测试；端侧包及通信协议版本不变。

## v0.22.3 - 2026-08-27

- 顶部首页、设备、地图、任务和指控大屏导航增加主题化图标，并保留原有文字与选中状态。
- 地图下发与任务保存下发按钮增加主题化上传图标，换肤时即时刷新且不改变按钮状态。
- 扩展图标资源、主题刷新和页面状态保持测试；端侧包及通信协议版本不变。

## v0.22.2 - 2026-08-27

- 导航常驻后将统一启动提前量从 30 秒缩短为 3 秒。
- Scout 在准备阶段拒绝 PGM 地图外、障碍区和未知区航点，避免执行中途因静态目标不可达而停止。
- `move_base` action 失败保留状态文本并返回明确错误码；`epgeneral_task_control` 升级至 v0.4.3。

## v0.22.1 - 2026-08-27

- 执行任务不再隐式保存并增加 revision，只允许对已下发且端侧导航 ready 的当前 revision 创建执行会话。
- 未 ready 的执行请求在创建会话和设备锁之前返回明确原因，避免重复点击提示“设备已有执行会话”。
- Scout 将 TF 查询和变换异常转换为 `LOCALIZATION_UNAVAILABLE` 反馈，任务准备失败不再形成 ROS callback 异常。
- `epgeneral_task_control` 升级至 v0.4.1；UDP 协议和端口不变。
- `epgeneral_task_control` v0.4.2 修复 Scout 导航适配器未持有 TF listener、无法读取系统实时 `map<-odom` 变换的问题；平台版本和 UDP 协议不变。

## v0.22.0 - 2026-08-27

- Scout 在任务文件完整提交后异步启动并常驻导航栈，导航就绪后端侧才报告 `ready`。
- 执行、完成、失败和常规停止复用现有 `/move_base`；删除、急停和关闭才停止导航进程。
- 平台将“文件已送达”和“导航已就绪”拆分，所有设备 ready 后才发送统一执行命令。
- `epgeneral_task_control` 升级至 v0.4.0；任务 UDP 协议、schema 和端口不变。

## v0.21.2 - 2026-08-27

- 返回设备、地图和任务列表的按钮统一为主题化返回图标与“返回”文字。
- 顶部主题切换按钮改用日间月亮、夜间太阳图标；指控大屏、地图设备栏和视频面板使用主题化展开/收起图标。
- 新增不依赖当前工作目录的应用图标加载器，八个 SVG 资源迁移至可发布的 `icons/app_icons/`。
- 新增图标加载、主题即时刷新、折叠方向和页面状态保持测试；端侧包及通信协议版本不变。

## v0.21.1 - 2026-08-26

- 修复任务二级页面重复显示两个“执行任务”按钮的问题，中间按钮改为“开始选点/结束选点”。
- Scout 执行前增加实时 `/fastlio_odom`、`map<-odom` TF 和导航地图文件校验。
- 端侧返回 `LOCALIZATION_UNAVAILABLE`、`MAP_FRAME_MISMATCH` 和 `NAVIGATION_STARTUP_TIMEOUT` 等明确错误码，平台日志保留设备错误详情。
- 重定位进程重启时自动清除历史 `localized` 状态，平台升级至 v0.21.1，任务控制至 v0.3.1，重定位至 v0.2.2。

## v0.21.0 - 2026-08-26

- 增加 Scout Mini 真实任务执行适配器：启动 `navigation_teb.launch`，通过 `/fastlio_odom` 与 `map<-odom` TF 获取位姿，并按顺序向 `/move_base/goal` 发送航点。
- 任务执行前校验任务地图、全局激活地图和端侧 localized 地图一致；常规终止和急停均取消导航目标并发布 `/cmd_vel` 零速度。
- `epgeneral_task_control` 升级至 v0.3.0；协议仍为 `ccs-task-control-v2` / schema 2。

## v0.20.1 - 2026-08-26

- 修正点云高度光标为保留光标以下点云，并同步实时点云筛选。
- 增加全局激活地图持久化，指控大屏地图选择作为唯一修改入口。
- 任务二级页面改为设备卡片操作，右侧点列表默认收起，选点按钮移动至右侧栏。

## v0.20.0 - 2026-08-26

- 指控平台和端侧任务控制升级至 `ccs-task-control-v2`，补充任务协商、读取、删除、终止和急停闭环。
- 端侧任务状态统一为 `no_task`、`task_exists`、`receiving`、`received`、`ready`、`running`、`completed`、`failed`、`emergency_stop`。
- 任务页面增加主任务控制和点云高度显示上限。

## v0.19.2 - 2026-08-25

- 修复重定位初始位姿选择器的方向十字标线被 VisPy 画布遮挡或尺寸不同步的问题；标线现在由地图视口的透明叠层布局管理。
- 初始位置仍必须位于当前地图范围内，yaw 方向采样改为投影到无边界地图平面，避免方向采样越出地图边缘时误报“无法从当前视图解算初始位姿”。
- 端侧协议和功能包未改动，继续配套 `epgeneral_relocalization` v0.2.1，无需重新部署 Scout 或 Go2。

## v0.19.1 / 端侧 relocalization v0.2.1 - 2026-08-25

- 重定位顶视选择器只在首次进入时复位相机；左键稳定调整 yaw，右键平移不再依赖 VisPy 残留手势状态，所有地图查看器改为以鼠标位置缩放。
- 地图二级页建图与重定位日志自动滚动至最新记录；在线设备卡的视频关闭后可折叠，启流时自动展开。
- Scout 重复重定位立即删除地面站绑定和端侧持久 TF，取消旧监测并反序重启栈；新 TF 先落端侧 schema 2 状态，再覆盖地面站绑定。
- Go2 同步 schema 2、旧 TF 清理和会话代际逻辑，但继续返回 `UNSUPPORTED_BACKEND`，不启动空后端。

## v0.19.0 / 端侧 udp_telemetry v0.3.0、relocalization v0.2.0 - 2026-08-25

- 设备详情统一显示活动地图重定位位姿、本地 odom 位姿和 IMU；Scout 使用 `/scout/odom`，FAST_LIO2 独立监测 `/Odometry`。
- 设备配置升级为 schema 6 并持久化 `active_map_id`；PGM 状态严格关联端侧活动地图目录中的普通 `map.pgm`。
- 删除八叉树和占据栅格状态卡，建图模式复用地图状态；UDP v1 增加精确 descriptor hash 允许列表以兼容滚动升级。
- Scout 固定 30 V 满电并记录 90 天分钟电压中位数；完整放电标定前百分比保持未知，Go2 继续使用原生 SOC。

- 修复地图下载、重定位栈启动和初始位姿处理期间，地面站收到合法进行中状态后仍累计重试次数，最终错误显示“端侧未响应指令”的问题；进度回包现在会刷新请求确认与重试预算。
- 地图在线设备卡新增地图下发、重定位状态、启动栈和初始位姿提交；固定中心十字星通过地图平移和旋转解算二维位置与 yaw。
- 新增 `ccs-relocalization-v1`：UDP 14565/14566 承载会话控制和状态，地面站 TCP 14601 提供带令牌、Range 和 SHA-256 的 PCD+PGM+YAML 地图 ZIP。
- 设备配置升级为 schema 5，按地图保存 `map <- odom` 完整变换；地图页、任务页和指控大屏可复用绑定显示在线设备。
- 新增 `epgeneral_relocalization` v0.1.0，Scout 按顺序管理 FAST-LIO、坐标适配、PCD 全局重定位和 map_server，并以稳定 TF 作为成功判据。
- Go2 profile 和地图目录约定已预留，但因缺少 Go2 全局重定位算法包保持禁用并返回 `UNSUPPORTED_BACKEND`。
- 自动化覆盖协议、配置迁移、地图包安全安装和 TF 稳定数学；2026-08-25 已在 Scout 完成部署、构建、常驻节点和 UDP 协商冒烟验证，真实地图传输、六阶段重定位栈、初始位姿与稳定 TF 仍需现场验收。

## 端侧 epgeneral_map_stream v0.9.1 - 2026-08-24

- 建图启动链新增 `go2_map_accumulator/map_accumulator.launch`，并等待 `/go2_map_accumulator` 节点就绪后才允许进入 mapping。
- 停止事务和 `ccs-map-stream-v2` 不变，仍按保存服务、新鲜度校验、停止进程和成果生成顺序执行。

## 端侧 epgeneral_map_stream v0.9.0 - 2026-08-24

- 停止建图先调用 `/go2_map_accumulator/save`，验证 `/maps/current/public_map.pcd` 为当前 session 新成果后，再终止 FAST_LIO 和坐标转换进程。
- 配置升级为 schema 6，新增保存 setup、服务名和 60 秒超时；保存失败统一 abort 清理，不进入 PGM/ZIP。
- start wrapper 不再删除已有 PCD，旧地图在新成果验证完成前保持不变。

## 端侧 epgeneral_map_stream v0.8.1 - 2026-08-24

- 取消建图启动阶段对 `odom <- lio_odom` 的可用性检测和数据记录，避免转换节点刚启动、TF 树尚未连通时返回 `cannot start mapping`。
- TF 仅在建图进入 mapping 且收到实际点云后，随点云时间戳查询并写入预览描述符；点云仍实际转换到 `odom`。
- v0.8.1 已部署至 `192.168.50.100`，端侧重建、53 项测试和版本一致性检查通过。

## v0.18.3 / 端侧 epgeneral_map_stream v0.8.0 - 2026-08-24

- 地图详情页新增可收起在线设备栏，显示名称、类型、ID、建图/任务状态、电量、坐标系及单路可控 SRT 视频。
- 地图复用 UDP 高频全局位姿显示在线设备标记，并按地图构建外参转换坐标；设备标记、选中设备坐标轴和有界轨迹作为独立图层。
- 静态与实时点云统一按高度使用低红高紫的 rainbow 色谱，并显示当前高度范围。
- 端侧按点云窗口参考时间查询 `odom <- lio_odom` TF，将实时 PCD 点坐标实际转换至 `odom` 后回传。
- PCD 描述符记录 `frame_id=odom`、`source_frame_id=lio_odom` 及七字段变换，平台严格校验并输出变换摘要。
- FAST_LIO 最终成果仍以 `lio_odom` 校验，完整提交后才定义为 `map`，避免实时显示帧与成果源帧混用。
- `epgeneral_map_stream` 配置升级至 schema 5、版本 v0.8.0；指控平台升级至 v0.18.3，协议 ID 保持 `ccs-map-stream-v2`。
- 已部署到 `192.168.50.100` 并完成 MID360 真机时间戳 TF 查询、start/abort 和残留清理验证。

## 端侧 epqrd_go2_bridge v0.2.0 - 2026-08-23

- 完整转发 Unitree SDK2 `rt/lowstate` 和 `rt/sportmodestate` 的全部字段，新增十二个 prefixed 强类型 ROS 话题。
- 增加 LowState 100 Hz、SportModeState 50 Hz 可配置限频，并保留 v0.1.0 的标准 ROS 兼容话题。
- 新增完整 ROS 话题接口、字段来源和订阅说明，配套 Go2 profile 同步升级。
- 已在 `QRD_001` 完成 Noetic 构建和 12 个新增话题真机验收，并将 Go2 DDS 网卡修正为 `eth1`。

## 端侧 epgeneral_map_stream v0.7.2 - 2026-08-23

- 建图流程调整为先启动并确认 FAST_LIO `/laserMapping`，再启动 TF manager、位姿适配器和两路点云坐标转换。
- 两阶段启动继续使用统一受控进程组，stop、abort、Ctrl+C 和成果保存行为保持不变。

## 端侧 epgeneral_map_stream v0.7.1 - 2026-08-23

- 修复坐标转换前置链加入后，建图协商仍以 1.5 秒运行 ROS 集成预检并误判超时的问题。
- 对 `prepare_result` 的错误摘要限长，避免预检异常文本导致 UDP 失败响应超过 1400 字节。

## 端侧 epgeneral_map_stream v0.7.0 - 2026-08-23

- 建图配置升级为 schema 4；新增外参文件、坐标转换 prerequisites launch 和就绪超时。
- v0.7.0 最初先启动 TF manager、位姿适配器和两路点云坐标适配器，后续 v0.7.2 已将顺序调整为 FAST_LIO 优先。
- 整条建图链统一纳入一个进程组，正常停止、强制结束和异常回滚均清理全部新增节点。

## v0.18.2 - 2026-08-22

### 新增

- `epgeneral_udp_telemetry` diagnostics 增加逐 ROS 来源的 topic、消息类型、接收/有效/拒绝计数和样本年龄，以及 Level 1/2/3 发送计数、字节数、失败数和序列。
- 端侧启动日志输出设备、session、目标地址、descriptor hash、分级频率和逐项订阅映射，并每 30 秒限频汇总来源统计。
- 增加端侧非法样本隔离、完整 Level 1 契约、localhost 20 Hz 接收及详情页最新快照绘制回归测试。

### 调整

- 指控平台升级至 `v0.18.2`，`epgeneral_udp_telemetry` 升级至 `v0.2.2`；`ccs-udp-telemetry-v1` wire schema 和 descriptor hash 保持兼容。
- 地面站按拒收原因累计并限频输出 UDP 协议告警，区分描述哈希、非有限数值、超长报文、解码、未知设备、旧会话和乱序。

### 修复

- 端侧 Pose/IMU 在进入平滑窗口前拒绝 `NaN/Inf` 和无效四元数；单个 descriptor 映射、平均或快照失败只将该项标记为无效，不再导致整组 Level 1 位姿与 IMU 被地面站拒收。
- 修正 UDP 接口文档中与实现不一致的数据年龄字段名，统一为 `sample_age_seconds`。

## v0.18.1 - 2026-08-22

- 修复 SRT 首帧到达后播放状态页覆盖真实视频画面的问题；播放状态仅触发一次，并增加首帧超时和 FFmpeg 诊断日志。
- v2 实时建图改为端侧每秒生成不可变二进制 PCD 分片，平台通过有界后台队列下载、校验、向量化合并并最多显示 30 万点。
- `epgeneral_map_stream` 升级至 v0.6.0；新增 `cloud_fragment_ready/cloud_fragment_ack`，UDP 不再承载 v2 实时点云数据。
- 最终成果生成前校验固定源 PCD 的 session 启动时间和文件指纹，拒绝下载未更新的旧地图。
- 修复 Go2 FAST_LIO 建图 launch 禁用 PCD 保存导致总是拒绝旧成果的问题；当前 session 固定输出验证后才原子更新公开 PCD，PGM 失败时恢复旧成果。
- 任务选点模式支持滚轮缩放。

## v0.18.0 - 2026-08-22

- 遥控建图接收缓冲提升为 4 MiB；不完整点云帧支持最多 3 次缺片选择性重传。
- 点云预览超时改为 10 秒警告、30 秒云与心跳同时中断才失败，持续心跳不再误判会话结束。
- 新增“强制结束”操作，通过 `abort_mapping` 停止端侧会话且不生成成果，随后可建立新 session。

## v0.17.0 - 2026-08-22

### 新增

- 遥控建图状态栏改为紧凑日志面板，按 session 保留最近 200 条 TX/RX/LOCAL 命令、重试、ACK、错误和限频点云摘要。
- `epgeneral_map_stream` v0.4.0 在启动 FAST_LIO 时立即回传 `session_status=starting`，并记录命令、状态转换、话题探测、子进程输出、耗时和退出码。

### 调整

- prepare 默认截止时间为 10 秒，start 和活动会话恢复为 45 秒；重试次数耗尽后仍接收截止时间内的有效晚到 ACK。
- 重新协商携带 `restart_active=true`。端侧对 ready/starting/mapping/error 会话执行无成果清理并从头准备；stopping/generating/serving 成果阶段保持不可中断。
- 实时协商、校验和显示以 `lio_odom` 为基准，最终成果下载并校验成功后切换为 `map`；`/livox/imu` 仅要求收到一条新数据。

### 修复

- 修复 FAST_LIO 启动超过原约 2.5 秒重试窗口时，指控端失败而端侧继续 mapping，继而重新协商返回活动会话冲突的问题。
- `epgeneral_map_stream` v0.4.1 修复同时间戳点云偶发先于位姿回调时被立即丢弃的问题，使用最多 3 帧的有界等待队列保持原 50 ms 同步精度。

## v0.16.1 - 2026-08-21

### 调整

- MQTT schema 1.0 增加向后兼容的可选 `session_id`，QoS 1 重复帧改为静默幂等处理，真正的回退序列仍会拒绝并告警。
- UDP 高频遥测统一按设备配置中的规范 ID 保存快照，并阻止已退役 session 的延迟包切回当前会话。

### 修复

- 修复端侧 MQTT 节点重启后 heartbeat/status sequence 归零，被持续运行的指控平台永久识别为乱序帧的问题。
- 修复 UDP 报文设备 ID 大小写与配置不同或旧会话包延迟到达时，高频位姿、IMU 和状态数据无法稳定显示的问题。
- 修复 Go2 EDU bringup 将 UDP 发往旧网段，以及一键脚本未传入 profile 设备/遥测配置，导致端侧以错误设备 ID 和话题发送高频数据的问题。
- 修复地图选点逆投影遗漏齐次坐标透视除法，导致屏幕像素位置未按 PCD/PGM 实际米制范围换算的问题；选点现限制在当前图层真实边界内。

## v0.16.0 - 2026-08-20

### 新增

- 新增 `ccs-map-stream-v2` 单机遥控建图协议与 preparing/ready/mapping/generating/downloading/validating 状态机。
- 新增端侧准备项结果、采样窗口、实时点云和全局位姿/轨迹显示。
- 新增带短期令牌 HTTP URL 的 Range 断点下载、ZIP 安全校验及 PCD/PGM/YAML 原子提交。
- 共享地图查看器新增点云/PGM/叠加切换、灰色坐标网格、刻度标签、光标坐标及持久化显示设置。
- 端侧 `epgeneral_map_stream` v0.3.0 新增 Livox 原始输入检查、FAST_LIO 生命周期控制和独立 PGM 生成脚本。

### 调整

- PGM 显示改为占据黑色、自由白色、未知 `205/255` 灰色，叠加模式使用半透明度。
- v0.16.0 UI 暂时隐藏多机实时建图入口；v1 多机后端与回归测试保留。
- 遥控建图活动期间锁定主导航，关闭应用需确认取消任务。
- 端侧 `epgeneral_map_stream` 升级至 v0.3.0；prepare 检查 `/livox/lidar` 与 `/livox/imu`，start 后验证 `/cloud_registered` 与 `/Odometry`。

### 修复

- 无。

### 删除

- 无。

## v0.15.1 - 2026-08-19

### 调整

- 融合算法注册表改为仅保存相对于 `data/map_fusion_algorithms` 的文件路径，不再绑定安装根目录层级。
- 建图服务接收算法对象时按算法 ID 从当前安装的仓储重新解析，工作进程只使用本次运行解析出的路径。

### 修复

- 修复软件换机、跨系统部署或移动安装目录后，旧运行时算法对象中的绝对路径导致融合插件无法启动的问题。
- 修复 Linux 与 Windows 路径格式互换时无法从旧绝对路径恢复受管算法文件的问题。

## v0.15.0 - 2026-08-19

### 新增

- 首页新增 NTP、MQTT Broker/订阅、UDP 遥测、建图、任务控制、FFmpeg/SRT 和地图/任务仓储状态卡。
- 设备编辑模式支持双击修改名称、类型、ID、IP 和 SRT 参数；设备 ID 可级联当前地图与未归档任务引用。
- 设备日志新增紧凑表格、当前设备一键清除，以及 MQTT、UDP、传感器和 SRT 状态变化记录。

### 调整

- 设备页面将飞行模式统一显示为运行模式，详情页移除 MAVLink 系统状态和解锁状态；底层 MQTT 字段保持兼容。
- 单机建图自动使用默认内置算法，不再向用户显示融合算法选择。
- 设备详情高频遥测改为缓存最新快照并按 20 Hz 合并绘制。

### 修复

- 修复 UDP 设备 ID 大小写差异或详情页重新显示后高频数据不刷新的问题。
- 修复指控大屏右侧状态栏被用户收起后，MQTT 快照更新又自动展开的问题。

## v0.14.1 - 2026-08-18

### 新增

- 指控平台新增内置 NTPv4 Server，默认监听 UDP 123，为无互联网局域网内的端侧设备授时。
- Go2 EDU 一键启动脚本在启动 ROS 功能包前强制执行 NTP 同步，并支持通过 `CCS_NTP_SERVER` 覆盖授时地址。

### 调整

- 端侧启动最多等待 30 秒确认 `systemd-timesyncd` 已从指定 CCS 地址完成同步；超时后退出，避免使用未对齐时间启动遥测、视频和控制组件。
- 一键启动脚本改为前台输出各阶段结果并持续监控节点；收到 `Ctrl+C` 后统一停止四个功能包及其受管 roslaunch 进程。

## v0.14.0 - 2026-08-18

### 新增

- 新增 `SrtFfmpegReceiver`、`SrtVideoWidget`、SRT URL 构建与系统 FFmpeg 协议检查。
- 设备配置 schema 升至 4，增加每设备 SRT 端口和延迟。
- 新增端侧 `epgeneral_video_srt` v0.1.0，订阅 ROS 图像并输出 H.264/MPEG-TS SRT Listener。

### 调整

- 视频运行基线调整为 Ubuntu 20.04、ROS Noetic、GStreamer 1.16+，端侧默认监听 UDP 9000。
- 地面站视频解码改用 `QProcess` 启动带 libsrt 的系统 FFmpeg，异常退出最多重试三次。

### 修复

- 修复 PySide6 内置 FFmpeg 不含 SRT 协议时无法连接端侧视频的问题，启动前提供明确能力错误。
- 修复设备切换、切页和退出时视频进程及重试计时器未统一清理的风险。

### 删除

- 删除地面站 `QMediaPlayer` RTSP 接收实现。
- 删除 `epgeneral_usb_cam_rtsp`、TCP 8554、RTSP Server 和 mount point 运行依赖。

## v0.13.2 - 2026-08-18

### 新增

- 新增通用本地静态资源路径解析器，统一设备类型图标和地图融合算法的存储与加载规则。
- 将当前配置引用的融合算法和设备图标纳入可部署的本地静态文件。

### 调整

- 导入算法和上传图标仍复制到 `data` 受管目录，运行时解析为绝对路径，持久化配置改为相对安装根目录的 POSIX 路径。

### 修复

- 修复仓库移动、换机或从不同工作目录启动后，融合算法因配置中的旧绝对路径而无法加载的问题。
- 旧绝对路径配置可按文件名在当前静态目录恢复，并在可写时自动迁移为相对路径。

### 删除

- 无。

## v0.13.1 - 2026-08-18

### 新增

- 已保存地图的“地图融合”弹窗新增“同步融合 PGM 图”选项。
- 同步模式复用点云融合的主地图与外参，使用 X/Y/Yaw 完成 PGM 逆向重采样与占据优先合并。

### 调整

- 勾选同步 PGM 时要求所有源地图均具有有效 PGM；地图列表直接标识 `PCD + PGM` 或“仅 PCD”。
- 融合结果的 PCD、PGM、YAML、点云来源和栅格来源元数据在同一次新地图提交中写入。

### 修复

- 同步 PGM 校验或写入失败时不再留下部分创建的融合地图。

### 删除

- 无。

## v0.13.0 - 2026-08-17

### 新增

- 地图页新增“PGM 融合”，支持选择目标 PCD、端侧设备、`source_map_id` 和 X/Y/Yaw 外参。
- 新增 `PgmArtifactProtocol`、`PgmDownloadCoordinator` 与 `PgmFusionEngine`，支持顺序下载、乱序分片、重复忽略、缺片补传、断点检查点及逆向最近邻融合。
- 地图 schema 升级为 5，记录目标 PCD 指纹、来源设备/地图、文件哈希、二维外参、输出分辨率和裁剪统计。

### 调整

- PGM 下载复用 UDP 14561/14562 和 `ccs-map-stream-v1`，并与实时建图互斥。
- 栅格冲突固定采用 `occupied > free > unknown`，输出绑定已有有效 PCD，且不修改点云文件。

### 修复

- PGM 融合提交失败或目标 PCD 中途变化时保留原有 PGM/YAML，不产生半提交图层。

### 删除

- 无。

## v0.12.0 - 2026-08-17

### 新增

- 新增可直接导入的纯 NumPy RANSAC 和 Open3D point-to-point ICP 地图融合插件。
- 新增从现有 PCD 生成 ROS map_server PGM/YAML 的后台处理流程，支持分辨率、高度、留白、点数、膨胀和空白区参数。
- 新增 `PcdToPgmOptions`、`PcdToPgmResult`、`PcdToPgmGenerator` 及地图仓储生成接口。

### 调整

- 地面站版本升级至 v0.12.0，并将 Open3D 0.18+ 纳入正式依赖。
- 地图与任务页面的数字输入框统一隐藏上下微调按钮，保留键盘输入和校验。

### 修复

- PGM 生成和替换复用仓储原子提交与旧图层恢复逻辑，处理失败不再影响现有地图图层。

### 删除

- 无。

## v0.11.1 - 2026-08-17

### 新增

- 新增共享地图拖动速度常量和可注入倍率参数，便于统一测试及后续配置。

### 调整

- 地面站版本从 v0.11.0 升级为 v0.11.1；所有端侧包及通信协议版本保持不变。
- 地图按钮 2 拖动的平面位移倍率统一提高至原来的 3 倍。

### 修复

- 修复大范围地图中按住右键拖动时视图移动过慢的问题；地图页、任务页和指控大屏同步生效。

### 删除

- 无。

## v0.11.0 - 2026-08-17

### 新增

- 新增离线 PCD 地图融合、主从 XYZ/RPY 外参和新地图原子提交。
- 新增标准 Python 融合插件仓储、独立进程执行器、内置体素算法及完整示例插件。
- 新增单机、多机、空地图三种创建模式，以及多设备建图任务与设备级状态快照。

### 调整

- 地图元数据升级至 schema 4，兼容读取 schema 1–3，并记录构建来源、算法指纹和主从关系。
- `MapBuildingService` 由单会话调整为一个逻辑任务下的多个独立设备会话；旧单机 API 保持兼容。
- `ccs-map-stream-v1` 的开始载荷增加可选 `job_id`、`role` 和 `primary_device_id`，端口与 schema 不变。

### 修复

- 多设备点云分别重组和累计后再转换到主坐标系，避免设备间序列、帧 ID 和轨迹互相污染。
- 插件失败或输出无效时保留临时结果，且不会覆盖已有正式 PCD。

### 删除

- 删除地图页原有只支持单设备的独立建图选择流程，统一使用建图向导。

## v0.10.1 - 2026-08-17

### 新增

- 新增与主题语义色同步的 Qt 全局 Palette，为原生弹出容器、视口和禁用控件提供背景兜底。
- 新增二级弹窗、下拉菜单和主题 Palette 回归测试。

### 调整

- 地面站版本从 v0.10.0 升级为 v0.10.1；端侧协议和所有端侧包版本保持不变。
- 对话框列表、树、表格、菜单及组合框弹层统一使用主题化背景、边框、悬停和选中状态。

### 修复

- 修复夜间主题下设备类型模板列表显示系统白色背景的问题。
- 修复组合框弹出菜单边缘白色漏底，以及其他二级对话框原生 viewport 可能绕过主题的问题。

### 删除

- 无。

## v0.10.0 - 2026-08-17

### 新增

- 新增 `config/device_types.json`、`DeviceTypeTemplateRepository` 和设备类型模板管理对话框，支持新增、编辑、引用删除保护及原子持久化。
- 新增 PNG/JPEG/SVG 类型图标上传、5 MiB 校验、应用内资产复制与未引用资源回收。
- 新增箭头、立方体和球体地图标记；箭头使用 UDP 全局位姿 yaw 定向，OpenGL 失败时回退圆点。
- 新增模板默认功能卡片与设备级动态继承/显式覆盖切换。
- 新增设备类型仓储、schema 迁移、继承语义及日间颜色映射测试。

### 调整

- 地面站版本从 v0.9.1 升级为 v0.10.0；端侧协议和所有端侧包版本保持不变。
- `devices.json` 升级至 schema 3，`status_cards: null` 表示继承模板，数组表示设备自定义覆盖。
- 新建设备类型选择改为读取模板仓储；设备卡优先显示模板图标。
- 地图页、任务页与指控大屏统一消费快照中的类型显示信息和地图形状。

### 修复

- 修复日间主题下选中设备卡黑底导致文字不可读的问题。
- 修复日间主题下地图按钮、任务二级页输入/列表/表格/滚动条及指控大屏按钮残留夜间颜色的问题。
- 修复模板或卡片更新可能重建 MQTT 快照并重置实时状态的风险。

### 删除

- 无。

## v0.9.1 - 2026-08-12

### 新增

- 新增端侧 `epgeneral_task_control` v0.1.0，实现 UDP 14563/14564 任务协议、XML 原子持久化、ID 状态机和 ROS 执行协调接口。
- 新增任务二级页面专用组件对象名和主题作用域，便于后续独立维护。

### 调整

- 地面站版本从 v0.9.0 升级为 v0.9.1；任务协议字段不变，新增独立版本的端侧 `epgeneral_task_control` v0.1.0。
- 统一设备列表、地图面板、航点表、冲突列表、日志列表、数值输入和分隔条的深色科技主题。

### 修复

- 修复任务二级页面多个 Qt viewport、表头和输入框继承系统白色调色板的问题。
- 修复浅色表格文字对比度不足及各功能区缺少视觉边界的问题。

### 删除

- 无。

## v0.9.0 - 2026-08-12

### 新增

- 新增持久化地图任务仓储、子任务/航点/冲突/执行模型、审计 JSONL、执行快照与永久事件日志。
- 新增任务列表、新建对话框、设备子任务编辑、PCD/PGM 选点、XYZ 修改、排序、地图折叠和实时设备标记。
- 新增连续时空轨迹冲突检查、冲突可视化及填写原因的强制共同执行。
- 新增 `config/task_system.json`、`ccs-task-control-v1`、UDP 14563/14564 分片下发、ACK 重试、缺片补发、统一 UTC 调度、停止和状态接收。
- 新增首页真实任务统计及指控大屏任务选择、地图联动、共同开始和停止。
- 新增任务仓储、冲突算法、协议编码和 localhost 假端侧集成测试。

### 调整

- 地面站版本从 v0.8.0 升级为 v0.9.0；现有端侧包版本保持不变。
- `PointCloudViewer` 扩展 browse/pick 交互和任务路径、冲突、执行标记接口。
- 首页不再使用模拟任务次数和最近任务；指控大屏任务控件不再是禁用占位。

### 修复

- 地图版本变化后要求人工复核，避免旧栅格航点未经确认直接执行。
- UDP 14564 绑定或配置失败时仅禁用下发执行，本地任务编辑仍可使用。

### 删除

- 删除任务页面与指控大屏的任务功能占位实现。

## v0.8.0 - 2026-08-05

### 新增

- 新增端侧 `epgeneral_map_stream` v0.1.0，支持 UDP 14561 建图控制、PointCloud2/位姿同步预处理及 UDP 14562 分片上传。
- 新增地图详情单设备实时建图入口、设备单选对话框和会话状态统计栏。
- 新增 `config/map_building.json` 与独立 `ccs-map-stream-v1` MessagePack 协议。
- 新增 UDP 14561 指令下发、14562 数据接收、命令重试、ACK、心跳和会话状态处理。
- 新增点云分片乱序重组、重复过滤、CRC32/zlib/资源限制校验及帧超时丢弃。
- 新增 `map <- body <- sensor` 坐标变换、体素运行质心融合和最高 5 Hz 实时预览。
- 新增 `.mapping` 会话检查点、轨迹 CSV、跨重启中断结果发现、保存与丢弃。
- 新增地图 schema 3 最近建图元数据和正式结果原子提交/回滚。
- 新增 `docs/EDGE_DEVICE_INTERFACES.md`，集中定义所有地面站与端侧接口。
- 新增协议核心、localhost UDP 集成、绑定失败降级和 schema 3 仓储测试。

### 调整

- 地面站版本从 `v0.7.1` 升级为 `v0.8.0`；新增独立版本的端侧 `epgeneral_map_stream` v0.1.0，其余端侧功能包版本保持不变。
- 地图 ZIP 在存在建图轨迹时包含 `trajectory.csv`。
- 返回地图列表、切换导航和退出应用时主动中断当前会话并保留临时结果。

### 修复

- 正式点云或元数据写入失败时同时恢复旧 PCD/轨迹，并保留新临时结果供重试。
- UDP 14562 端口绑定失败时关闭已创建 socket，仅禁用建图模块而不影响主程序。

### 删除

- 无。

## v0.7.1 - 2026-08-04

### 新增

- 新增共享三维查看器的鼠标中键按住拖动平移操作。
- 新增在线设备栏详情、摘要和完全收起三态，以及窄边恢复入口。
- 新增控制台完全收起和标题条恢复入口。
- 新增相机平移、全部在线设备、图表标题、折叠状态和 splitter 空间释放测试。

### 调整

- 地面站版本从 `v0.7.0` 升级为 `v0.7.1`；端侧功能包版本保持不变。
- 在线列表从仅 UAV 调整为所有 MQTT 在线设备，相关界面文字统一为“设备”。
- 位置和姿态图表改用紧凑标题栏与自定义小型图例，扩大坐标绘图区。
- 设备栏和控制台折叠状态在当前应用运行期间保持，重启后恢复默认布局。

### 修复

- 修复 Qt Charts 默认标题和图例占用过多纵向空间的问题。
- 修复 VisPy 中键拖动默认执行缩放、无法直接平移地图的问题。
- 修复设备栏视觉收起后 splitter 仍保留原宽度的问题。

### 删除

- 无。

## v0.7.0 - 2026-08-04

### 新增

- 新增科技化指控大屏、倒梯形标题、系统时间及 MQTT/UDP 模块状态摘要。
- 新增仅展示在线 UAV 的可收起设备栏，以及设备离线后的稳定选择迁移。
- 新增设备实时状态栏、60 秒/1200 样本位置与姿态趋势图。
- 新增 PCD、PGM 和叠加三种数字孪生图层模式，以及设备朝向轴和轨迹显示。
- 新增 ROS map_server YAML 与 P2/P5 PGM 导入、校验、持久化和 ZIP 导出。
- 新增大屏沉浸全屏、Esc 退出、控制台地图/图层/视角/扫描控制和任务占位控件。
- 新增趋势缓冲、PGM 解析、地图 schema 兼容、大屏生命周期及响应式布局测试。

### 调整

- 地面站版本从 `v0.6.1` 升级为 `v0.7.0`；端侧功能包版本保持不变。
- 地图元数据写入 schema 2，同时继续兼容读取 schema 1 PCD 地图。
- 地图就绪条件调整为至少存在一个有效 PCD 或 PGM 图层。
- `PointCloudViewer` 扩展为可复用的多图层数字孪生渲染组件。

### 修复

- PGM 双文件替换失败时完整恢复旧图层，避免只提交新 PGM 或遗留备份。
- 宽屏状态栏现在按窗口尺寸自动展开，同时尊重用户主动收起选择。
- 页面隐藏、切换导航和退出全屏时停止不需要的图表及装饰动画。

### 删除

- 删除“指控大屏”原“功能开发中”占位内容。

## v0.6.1 - 2026-08-03

### 新增

- 新增新建地图设备选择区的主题回归测试与离屏截图验证。

### 调整

- 地面站版本从 `v0.6.0` 升级至 `v0.6.1`。
- 地图创建对话框为设备列表滚动区域增加独立主题选择器。

### 修复

- 修复新建地图对话框的设备列表白底白字，导致内容不可见的问题。

### 删除

- 无。

## v0.6.0 - 2026-08-03

### 新增

- 新增项目内 `data/map_server` 地图仓储、schema 1 元数据和地图状态模型。
- 新增地图卡片列表、名称搜索、新建对话框和多设备建图信息保存。
- 新增地图重命名、PCD 导入或替换、ZIP 下载及 `.trash` 回收删除。
- 新增 pypcd4 PCD 解析、NumPy 范围计算和确定性显示降采样。
- 新增 VisPy 三维点云查看器、视角复位、适配全部和设备标记预留接口。
- 新增地图仓储、PCD 边界、损坏目录隔离、ZIP 和回收删除测试。

### 调整

- 地面站版本从 `v0.5.1` 升级为 `v0.6.0`；端侧包版本保持不变。
- 地图页由三栏二维模拟视图改为“地图列表 / 三维详情”二级页面。
- 首页地图数量改为由持久化地图仓储实时提供，不再使用静态模拟概览。
- 新增 NumPy、VisPy 和 pypcd4 地面站依赖。

### 修复

- 地图数据现在可跨应用重启保存，损坏地图不会阻止其他地图加载。
- PCD 导入和元数据写入采用临时文件校验与原子替换，避免失败操作破坏有效地图。
- OpenGL 或三维依赖不可用时仅禁用地图渲染，不阻止主程序启动。

### 删除

- 删除旧地图页的 `QGraphicsView` 二维坐标网格、静态地图列表和模拟设备点位显示。

## v0.5.1 - 2026-08-02

### 新增

- 新增端侧源码入口独立导入测试和 catkin 测试注册。
- README 新增 Melodic 工作空间重新构建、source 和 Python 导入检查命令。

### 调整

- 地面站版本从 `v0.5.0` 升至 `v0.5.1`；`epgeneral_udp_telemetry` 从 `v0.2.0` 升至 `v0.2.1`。
- CMake 强制缓存选定的 Python 3.6+ 解释器，统一 `catkin_python_setup` 与运行脚本环境。

### 修复

- 修复 roslaunch 直接执行源码目录脚本时无法导入同包 `epgeneral_udp_telemetry` 模块的问题。
- 修复 Melodic CMake 缓存可能继续引用 Python 2、导致 devel Python 路径生成不一致的问题。

### 删除

- 无。

## v0.5.0 - 2026-08-02

### 新增

- 新增设备绑定的 `status_cards` 配置和固定六类状态卡目录。
- 新增状态卡编辑对话框，支持全选、清空、组合选择和立即持久化。
- 新增 Livox、FAST-LIO2、PGM、八叉树、占据栅格和当前建图模式卡片。
- 新增 UDP `text_status` 类型及端侧可配置 ROS 文本字段采集。
- 新增 schema 迁移、卡片持久化、运行态保持、文本状态和响应式 UI 测试。

### 调整

- 地面站版本从 `v0.4.0` 升级至 `v0.5.0`；`epgeneral_udp_telemetry` 从 `v0.1.0` 升级至 `v0.2.0`。
- `devices.json` 从 schema 1 升级至 schema 2，旧配置首次读取时自动迁移。
- 数据接收状态由单个汇总面板改为轻量独立卡片网格，按宽度切换 1/2/3 列。
- Livox 状态卡合并显示驱动可用性和点云接收频率；建图模式显示最近文本值。
- 默认端侧三级话题由 Ego-Planner 状态调整为 PGM、OctoMap、OccupancyGrid 和 mapping mode。

### 修复

- 修复状态项目集中在单行中导致信息层级不清、长文本易裁切的问题。
- 修复所有设备共享同一状态项目集合、无法按设备能力配置的问题。
- 修复编辑静态展示配置可能重建 MQTT 快照并丢失运行状态的风险。

### 删除

- 删除旧的数据接收汇总大面板和单行状态标签。
- 删除默认 Ego-Planner 状态项；仍可在后续扩展目录中重新加入。

## v0.4.0 - 2026-08-01

### 新增

- 新增 UDP 14560 高频遥测配置、MessagePack 协议、描述哈希和严格数据报校验。
- 新增独立 UDP 接收线程、设备遥测仓储、1 Hz 心跳及 2 秒 warning/5 秒 offline 状态机。
- 新增全局 ENU 位姿、视觉位姿、IMU、点云接收状态和三级传感器可用性模型。
- 设备详情页新增 20 Hz 遥测面板、UDP 链路字段、点云元数据和红绿灰状态灯。
- 新增端侧 `epgeneral_udp_telemetry v0.1.0` ROS 包，支持 MAVROS 默认话题、普通 ROS 字段映射、窗口平滑和低频样本复用。
- 新增地面站/端侧配置一致性、协议边界、心跳状态机、采样平滑和 UI 测试。

### 调整

- 地面站版本从 `v0.3.0` 升级为 `v0.4.0`，新增 `msgpack` 依赖。
- 高频 UDP 快照与 MQTT 设备摘要分离，详情页按 50 ms 定时器绘制，设备网格不随 20 Hz 数据重建。
- 设备日志源增加外部链路事件入口，UDP warning/error/recovery 可使用现有等级筛选。
- 更新端侧部署说明、UDP 防火墙、ROS 话题映射和故障排查文档。

### 修复

- 显式以 UTF-8 读取端侧 YAML，避免非 UTF-8 系统区域设置无法解析中文显示名称。
- 通过会话 ID 隔离设备重启前后的 UDP sequence，避免新进程从零计数时被误判为乱序。
- UDP 端口绑定失败时只标记模块故障，不阻止 MQTT、RTSP 和主界面运行。

### 删除

- 删除设备详情“地图位置”字段；地图页面和位置数据模型不变。

## v0.3.0 - 2026-07-31

### 新增

- 设备详情页新增按需 RTSP 视频区域和视频流开关，固定地址为 `rtsp://<设备IP>:8554/usb_cam`。
- 新增 Qt Multimedia 播放状态、错误状态和设备切换时的播放器释放。
- 新增宽屏左右、窄屏上下的详情页响应式布局。
- 新增 `edge_side_pkg` 端侧部署目录、`epgeneral_device_config` 共享设备配置包和 `epgeneral_usb_cam_rtsp` ROS/GStreamer 推流包。
- 新增端侧 USB 摄像头默认配置、RTSP launch、H.264 低延迟管线和断帧日志。
- 新增 RTSP URL、假播放器生命周期、布局、共享配置对齐和端侧视频配置测试。

### 调整

- 地面站版本从 `v0.2.0` 升级为 `v0.3.0`。
- `epgeneral_mqtav` 从根目录迁移到 `edge_side_pkg/epgeneral_mqtav`，版本从 `v0.2.2` 升级为 `v0.3.0`。
- `epgeneral_mqtav` 不再在自身配置中重复保存设备 ID/IP，改从 `epgeneral_device_config/config/device.yaml` 读取。
- 根目录发布归档迁移并重新生成到 `edge_side_pkg/epgeneral_mqtav.zip`。
- README、开发笔记和端侧包文档同步更新 Ubuntu 18.04、ROS Melodic、RTSP 8554 和共享配置说明。

### 修复

- 修复详情页 MQTT 快照刷新时重复重启视频流的问题。
- 修复离开详情页或切换主导航后媒体播放器仍占用连接的风险。
- 修复端侧视频慢客户端可能阻塞 ROS 图像回调的问题，改用有界丢帧队列。

### 删除

- 删除根目录旧 `epgeneral_mqtav` 源码路径和旧归档副本。
- 删除 `epgeneral_mqtav` 自身配置中的重复 `device` 配置块。

## v0.2.0 - 2026-07-31

### 新增

- 新增 `amqtt` 内嵌 Broker、`paho-mqtt` 订阅器和统一 MQTT 运行时生命周期。
- 新增 `config/mqtt.json`，配置监听地址、端口、QoS、主题、心跳阈值和日志容量。
- 新增 mqtav schema 1.0 的 presence、heartbeat、status 严格解析器和事件模型。
- 新增 MQTT 设备数据源、1 Hz 心跳检查、2 秒 warning、5 秒 offline/error 及恢复状态机。
- 新增每设备 500 条环形实时日志，记录连接、心跳、状态帧、断联和恢复事件。
- 新增飞行模式、解锁状态、MAVLink system status、电池电压/电流、原始任务状态和最后心跳字段。
- 新增 MQTT 配置、协议、状态机、真实 Broker 收发、端口冲突、线程关闭和 UI 联动测试。

### 调整

- 地面站版本从 `v0.1.0` 升级为 `v0.2.0`；`epgeneral_mqtav` 端侧包保持 `v0.1.0`。
- 默认运行时数据源由模拟遥测切换为 MQTT，启动设备状态统一为离线和未知。
- 首页、设备页和地图页统一依赖 `DeviceDataSource` 协议，不直接依赖模拟源或 MQTT 库。
- 设备页连接标签改为展示 Broker/订阅模块状态，设备卡片增加飞行模式。
- 详情页扩展全部 MQTT 遥测字段，健康状态改为直接反映 FCU 链路。
- README 增加可信内网部署、TCP 1883、防火墙、机载配置、协议和故障排查说明。

### 修复

- 修复 Paho v2 `ReasonCode` 成功连接被误判为拒绝的问题。
- 修复 Broker 绑定失败后退出时访问已关闭 asyncio 事件循环的问题。
- 修复 Broker 线程刚启动、事件循环尚未就绪时立即退出可能遗留后台线程的问题。
- 修复 Broker 已启动但订阅尚未建立时模块状态含义不清的问题。
- 修复 MQTT 新建设备过程中短暂生成模拟日志的问题。
- 修复浮点电量无法正确设置 Qt 进度条和显示格式的问题。

### 删除

- 无用户功能删除；默认界面不再使用模拟遥测作为运行时真值。

## v0.1.0 - 2026-07-30

### 新增

- 新增 JSON 设备配置、结构校验、默认配置初始化和原子写入。
- 新增设备静态档案、可用性、健康状态和三级日志数据模型。
- 新增设备编辑模式、卡片复选框、批量删除与二次确认。
- 新增设备创建对话框、重复 ID 检查、IPv4/IPv6 校验和异步 ping 测试。
- 新增设备详情二级页面、完整信息展示和 `info`、`warning`、`error` 日志筛选。
- 新增配置、数据源、ping 和设备管理 UI 自动化测试。

### 调整

- 版本号从 `v0.0.2` 升级为 `v0.1.0`。
- 设备静态信息从 Python 硬编码迁移到 `config/devices.json`。
- 设备数据源改为合并静态档案与模拟运行时遥测，并提供创建、删除、详情和日志接口。
- `DeviceSnapshot` 增加 IP、最近可用状态、最后测试时间和健康状态。
- 设备卡片增加双击详情信号和编辑复选状态。
- README 和开发笔记同步扩展设备管理与配置说明。

### 修复

- 避免重复设备 ID、非法 IP 或未经当前 IP 测试的设备写入配置。
- 避免配置损坏时自动覆盖用户原文件。
- 避免 ping 命令阻塞 UI 线程或通过 shell 拼接输入。

### 删除

- 删除设备静态信息的 Python 硬编码列表，改由 JSON 配置提供。

## v0.0.2 - 2026-07-30

### 新增

- 新增首页、设备、地图、任务和指控大屏五项顶部导航。
- 新增系统首页，展示版本、设备状态、地图数量、任务次数和上次任务总结。
- 新增地图三栏页面、模拟地图目录、本地 XY 设备点位、滚轮缩放和拖拽平移。
- 新增 `MapDefinition`、`TaskExecutionSummary` 和 `SystemOverview` 数据模型。
- 新增统一版本常量并同步到应用元数据、窗口标题和首页。
- 新增 Qt 离屏界面测试、开发笔记和版本更新记录。

### 调整

- 将 v0.0.1 单页设备界面重构为独立 `DevicesPage`。
- 将主窗口调整为导航外壳和堆叠页面容器。
- 扩展 `DeviceSnapshot`，增加可选本地位置和 `frame_id`。
- 首页与设备源联动更新在线、离线数量。
- 设备页统计、工具栏和卡片网格改为响应式布局。
- README 改为完整中文功能、安装、使用、测试和 ROS 接入说明。

### 修复

- 修复窗口缩小时固定宽度统计卡和搜索工具无法合理重排的问题。
- 修复页面业务全部集中在主窗口、难以继续扩展的问题。
- 修复版本信息缺少单一来源的问题。
- 修复动态重排设备卡片时旧控件覆盖新布局的问题。
- 修复设备类型自绘控件在部分 PySide6 离屏绘制环境中触发原生访问冲突的问题。
- 修复打包 Qt 运行时未自动发现 Windows 中文字体时文字显示为方框的问题。
- 修复地图侧栏和画布显示不必要滚动条的问题。

### 删除

- 无。

## v0.0.1 - 2026-07-30

### 新增

- 新增 PySide6 设备链接与显示首屏。
- 新增设备卡片、状态统计、搜索、类型筛选、连接筛选和刷新功能。
- 新增模拟设备数据源、`DeviceSnapshot` 模型和 ROS1/ROS2 转换接口。
- 新增模型与适配器基础测试。

### 调整

- 无。

### 修复

- 无。

### 删除

- 无。
